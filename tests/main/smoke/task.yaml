summary: terraform SDK installs and reports a version

execute: |
  workshop launch --verbose --wait-on-error
  workshop info | grep -E 'status:\s+okay'
  workshop exec -- bash -lc 'terraform version' | grep -E 'Terraform v'
